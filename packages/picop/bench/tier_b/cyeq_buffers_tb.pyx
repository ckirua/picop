# cython: language_level=3, boundscheck=False, wraparound=False, initializedcheck=False
# Auto-generated by _gen_eq_inventory.py — do not hand-edit.
from cypy.cybytes cimport bytes_eq, bytes_bytearray_eq
from cypy.cybytearray cimport bytearray_eq
from cypy.cyarray cimport array_eq
from cypy.cymemoryview cimport memoryview_eq
from cypy.cybuffer cimport buf_eq
from cypy.cystr cimport str_eq
from cypy.cyunicode cimport unicode_eq
from cpython.array cimport array
include "_sink.pxi"


cpdef bint baseline_bytes_eq(bytes a, bytes b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = <bytes>tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_bytes_eq(bytes a, bytes b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = bytes_eq(<bytes>tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_bytes_bytearray_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_bytes_bytearray_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = bytes_bytearray_eq(tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_bytearray_eq(bytearray a, bytearray b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = <bytearray>tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_bytearray_eq(bytearray a, bytearray b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = bytearray_eq(<bytearray>tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_array_eq(array a, array b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = <array>tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_array_eq(array a, array b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = array_eq(<array>tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_memoryview_eq(memoryview a, memoryview b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = <memoryview>tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_memoryview_eq(memoryview a, memoryview b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = memoryview_eq(<memoryview>tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_buf_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_buf_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = buf_eq(tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_str_eq(str a, str b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = <str>tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_str_eq(str a, str b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = str_eq(<str>tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_unicode_eq(str a, str b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = <str>tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_unicode_eq(str a, str b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = unicode_eq(<str>tb_obj(a, k), b)
        tb_sink_bint(r)
    return r

