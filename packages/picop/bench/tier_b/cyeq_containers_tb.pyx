# cython: language_level=3, boundscheck=False, wraparound=False, initializedcheck=False
# Auto-generated by _gen_eq_inventory.py — do not hand-edit.
from cypy.cylist cimport list_eq
from cypy.cytuple cimport tuple_eq
from cypy.cysequence cimport seq_eq
from cypy.cydict cimport dict_eq
from cypy.cyset cimport set_eq, frozenset_eq
from cypy.cymapping cimport map_eq
from cypy.cydeque cimport deque_eq
from cypy.cyrange cimport range_eq
include "_sink.pxi"


cpdef bint baseline_list_eq(list a, list b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = <list>tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_list_eq(list a, list b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = list_eq(<list>tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_tuple_eq(tuple a, tuple b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = <tuple>tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_tuple_eq(tuple a, tuple b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = tuple_eq(<tuple>tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_seq_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_seq_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = seq_eq(tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_dict_eq(dict a, dict b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = <dict>tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_dict_eq(dict a, dict b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = dict_eq(<dict>tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_set_eq(set a, set b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = <set>tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_set_eq(set a, set b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = set_eq(<set>tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_frozenset_eq(frozenset a, frozenset b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = <frozenset>tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_frozenset_eq(frozenset a, frozenset b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = frozenset_eq(<frozenset>tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_map_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_map_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = map_eq(tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_deque_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_deque_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = deque_eq(tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_range_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_range_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = range_eq(tb_obj(a, k), b)
        tb_sink_bint(r)
    return r

