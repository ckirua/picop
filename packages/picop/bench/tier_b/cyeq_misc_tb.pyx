# cython: language_level=3, boundscheck=False, wraparound=False, initializedcheck=False
# Auto-generated by _gen_eq_inventory.py — do not hand-edit.
from cypy.cytype cimport type_eq
from cypy.cycellobject cimport cell_eq
from cypy.cyobject cimport obj_eq
from cypy.cyfunction cimport func_eq
from cypy.cymethod cimport method_eq
from cypy.cymodule cimport mod_eq
from cypy.cygenobject cimport gen_eq
from cypy.cyiterator cimport iter_eq
from cypy.cypycapsule cimport capsule_eq
from cypy.cyweakref cimport weakref_eq
from cypy.cycontextvars cimport context_eq
include "_sink.pxi"


cpdef bint baseline_type_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_type_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = type_eq(tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_cell_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_cell_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = cell_eq(tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_obj_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_obj_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = obj_eq(tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_func_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_func_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = func_eq(tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_method_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_method_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = method_eq(tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_mod_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_mod_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = mod_eq(tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_gen_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_gen_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = gen_eq(tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_iter_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_iter_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = iter_eq(tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_capsule_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_capsule_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = capsule_eq(tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_weakref_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_weakref_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = weakref_eq(tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_context_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_context_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = context_eq(tb_obj(a, k), b)
        tb_sink_bint(r)
    return r

