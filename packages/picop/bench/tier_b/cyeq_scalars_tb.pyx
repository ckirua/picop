# cython: language_level=3, boundscheck=False, wraparound=False, initializedcheck=False
# Auto-generated by _gen_eq_inventory.py — do not hand-edit.
from cypy.cybool cimport bool_eq
from cypy.cyfloat cimport float_eq
from cypy.cylong cimport long_eq, int_eq
from cypy.cycomplex cimport complex_eq
from cypy.cynumber cimport num_eq
from cypy.cyslice cimport slice_eq
from cypy.cydatetime cimport dt_date_eq, dt_time_eq, dt_datetime_eq, dt_timedelta_eq
from cpython.datetime cimport import_datetime

import_datetime()
include "_sink.pxi"


cpdef bint baseline_bool_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_bool_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = bool_eq(tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_float_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_float_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = float_eq(tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_long_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_long_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = long_eq(tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_int_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_int_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = int_eq(tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_complex_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_complex_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = complex_eq(tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_num_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_num_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = num_eq(tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_slice_eq(slice a, slice b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = <slice>tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_slice_eq(slice a, slice b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = slice_eq(<slice>tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_dt_date_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_dt_date_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = dt_date_eq(tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_dt_time_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_dt_time_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = dt_time_eq(tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_dt_datetime_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_dt_datetime_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = dt_datetime_eq(tb_obj(a, k), b)
        tb_sink_bint(r)
    return r


cpdef bint baseline_dt_timedelta_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = tb_obj(a, k) == b
        tb_sink_bint(r)
    return r

cpdef bint cypy_dt_timedelta_eq(object a, object b, Py_ssize_t n):
    cdef Py_ssize_t k
    cdef bint r = False
    for k in range(n):
        r = dt_timedelta_eq(tb_obj(a, k), b)
        tb_sink_bint(r)
    return r

